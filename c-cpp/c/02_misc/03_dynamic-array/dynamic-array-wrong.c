#include <stdio.h>
#include <stdlib.h>

int main(){

  int size = 10;
  int arr[size];
  for (int i = 0; i < size; i++) {
    arr[i] = i * 3;
    printf("%d,", arr[i]);
  }

  size = 20
  arr[size];
  for (int i = 0; i < size; i++) {
    arr[i] = i * 3;
    printf("%d,", arr[i]);
  }
  return 0;
}